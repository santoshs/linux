if ARCH_KOTAEVM

config ARCH_TEXT_BASE
	hex
	default 0x43000000

config MACH_KOTAEVM
	bool "KOTA EVM board"
	default y
	select MACH_HAS_LOWLEVEL_INIT
	help
	  This adds generic code to configure the CPG, GPIO, SDRAM, etc after reset.
	
config BOARDINFO
	default "Renesas KOTAEVM"

source arch/arm/boards/kotaevm/Kconfig 

endif
