#
# Sensor device configuration
#

menu "I2C Hardware Bus support"

config I2C_IMX
	bool "i.MX I2C Master driver"
	depends on ARCH_IMX

config I2C_OMAP
	bool "OMAP I2C Master driver"
	depends on ARCH_OMAP

config R_MOBILE_I2C
	bool "R-Mobile I2C Master driver"
	depends on ARCH_U2EVM
	
endmenu
