#----------------------------------------------------------------
# command
#----------------------------------------------------------------
LD = arm-eabi-ld
CONV = arm-eabi-objcopy

#----------------------------------------------------------------
# Build options.
#----------------------------------------------------------------
LDFLAGS     = $(LDOPT)
CONVFLAGS   = $(CONVOPT)
LDROULE     = $(LD) $(LDFLAGS)
CONVROULE   = $(CONV) $(CONVFLAGS)
